function calc(obj) {
     const value = 50 * obj.x
     return value + obj.y + obj.z
}

output = calc({
     x: 10,
     y: 120,
     z: 2330
})

output = calc({
     x: 10,
     y: 220,
     z: -30
})

output = calc({
     x: 10,
     y: 20
})

output = calc({
     x: 10,
     y: -20,
     z: 30
})

var x = 15
let y = 250 * x
const z = x + y

let name = "Mukhammaddiyor"
let greet = "Hello, "
let bye = "and bye bye!"

// ---------------------------
